# Configuración de Django
# Copia este archivo a .env y completa los valores según tu entorno

# Clave secreta de Django (generar una nueva para producción)
# Puedes generar una con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure--+y3u#m7c5hake)vd39o+8=u&e#a&80kwja^)o*!55jnz$#ee9

# Modo debug (True para desarrollo, False para producción)
DEBUG=True

# Hosts permitidos (separados por comas)
# Ejemplo: ALLOWED_HOSTS=localhost,127.0.0.1,ejemplo.com
ALLOWED_HOSTS=

# Configuración de base de datos
# Para SQLite (desarrollo):
# DATABASE_URL=

# Para PostgreSQL (producción):
# DATABASE_URL=postgresql://usuario:password@localhost:5432/nombre_bd

# Configuración de email (opcional, para futuras notificaciones)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu_email@gmail.com
# EMAIL_HOST_PASSWORD=tu_password
# DEFAULT_FROM_EMAIL=noreply@gestor-notas.com